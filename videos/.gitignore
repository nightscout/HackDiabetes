# HyperFrames CLI working/cache dir — snapshots, check PNGs, render scratch.
# Regenerated by the CLI; the tracked source is hyperframes.json + compositions.
.hyperframes/

# (logo-animation/ ignores its own regenerated out/ via its local .gitignore)
